This is a mediawiki extension that configures the Dashiki sub-namespace to the Config namespace.  It makes Dashiki configurations easier to read and provides convenient functionality to work with Dashiki dashboards.  For now, it's bare-bones.

Install
=======

To install, add the following to LocalSettings.php

	wfLoadExtension( 'Dashiki' );
